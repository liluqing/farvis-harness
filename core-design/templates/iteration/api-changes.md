# 接口变更记录

> **迭代**：{YYYY-MM-DD_需求名称_版本}
> **最后更新**：{YYYY-MM-DD}

---

## 变更清单

### 新增接口

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| POST | /api/v1/{resource} | {接口用途} | 已实现 / 待实现 |
| GET | /api/v1/{resource}/{id} | {接口用途} | 已实现 / 待实现 |

### 修改接口

| 方法 | 路径 | 变更内容 | 兼容性 |
|------|------|---------|--------|
| PUT | /api/v1/{resource}/{id} | {变更说明} | 兼容 / 不兼容 |

### 废弃接口

| 方法 | 路径 | 废弃原因 | 替代方案 |
|------|------|---------|---------|
| DELETE | /api/v1/{resource}/{id} | {原因} | {替代接口} |

---

## 接口详情

> 仅列出核心字段，完整定义见代码中的 DTO

### {POST /api/v1/{resource}}

**请求体**：
```json
{
  "field1": "string",
  "field2": 0
}
```

**响应体**：
```json
{
  "code": 0,
  "data": {
    "id": 0
  }
}
```

---

## 归档时合并到 `project/api-contracts.md` 的要点

- 新增接口：追加到对应模块的接口列表
- 修改接口：更新对应接口的字段/状态
- 废弃接口：标记为 deprecated，记录废弃日期
