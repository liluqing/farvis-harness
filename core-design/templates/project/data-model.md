# 数据模型

> **最后更新**：{YYYY-MM-DD}（迭代 {迭代名} 归档）
> **维护规则**：仅在迭代归档时更新，迭代进行中保持不变

---

## 1. ER 关系概览

> 核心实体及其关系

```
┌──────────┐       ┌──────────┐       ┌──────────┐
│  User    │ 1───N │  Order   │ 1───N │ OrderItem│
└──────────┘       └──────────┘       └──────────┘
```

---

## 2. 表结构总览

> 所有当前有效的表，按模块分组

### 模块：{module-a}

| 表名 | 用途 | 核心字段 | 关联表 |
|------|------|---------|--------|
| {table_1} | {用途} | id, name, status | {关联表} |
| {table_2} | {用途} | id, {table_1}_id, amount | {table_1} |

### 模块：{module-b}

| 表名 | 用途 | 核心字段 | 关联表 |
|------|------|---------|--------|
| {table_3} | {用途} | id, ... | 无 |

---

## 3. 变更历史

| 迭代 | 日期 | 变更类型 | 对象 | 说明 |
|------|------|---------|------|------|
| {迭代名} | {YYYY-MM-DD} | 新增表 | {table_name} | {说明} |
| {迭代名} | {YYYY-MM-DD} | 新增字段 | {table.field} | {说明} |
| {迭代名} | {YYYY-MM-DD} | 废弃字段 | {table.field} | {说明} |
