# 架构设计: <!-- 项目/功能名 -->

> **版本：** v1.0 · <!-- 日期 -->
> **关联 PRD：** <!-- prd/xxx-prd.md -->
> **关联任务清单：** <!-- task-list-xxx.md -->

---

## 1. 技术栈

| 维度 | 选型 | 版本 | 选型依据 |
|------|------|------|------|
| 语言/运行时 | <!-- Java / Python / Go / ... --> | <!-- 17 / 3.12 / 1.22 --> | <!-- 为什么 --> |
| 框架 | <!-- Spring Boot / FastAPI / ... --> | <!-- 3.x --> | <!-- 为什么 --> |
| 数据库 | <!-- MySQL / PostgreSQL / ... --> | <!-- 8.0 / 15 --> | <!-- 为什么 --> |
| ORM | <!-- JPA / SQLAlchemy / ... --> | — | <!-- 为什么 --> |
| 缓存 | <!-- Redis / Caffeine / 无 --> | — | <!-- 为什么 --> |
| 消息队列 | <!-- RabbitMQ / Kafka / 无 --> | — | <!-- 为什么 --> |
| 部署 | <!-- Docker Compose / K8s / 云 --> | — | <!-- 为什么 --> |

---

## 2. 模块划分

### 模块总览

```
<!-- 项目名 -->
├── <!-- 模块 1 -->  — <!-- 一句话职责 -->
├── <!-- 模块 2 -->  — <!-- 一句话职责 -->
└── <!-- 模块 3 -->  — <!-- 一句话职责 -->
```

### 模块详情

#### 模块: <!-- 模块名 -->

| 维度 | 说明 |
|------|------|
| **职责** | <!-- 一句话 --> |
| **核心实体** | <!-- Entity 1, Entity 2 --> |
| **对外接口** | <!-- REST API / gRPC / 事件 --> |
| **依赖模块** | <!-- 依赖哪些模块 --> |
| **被依赖** | <!-- 被哪些模块依赖 --> |

#### 模块: <!-- 模块名 -->

<!-- 同上 -->

---

## 3. 通信架构

```
<!-- 用 ASCII 图展示模块间通信关系 -->

用户请求 → [API Gateway] → 模块 1
                              ├── REST → 模块 2
                              └── Event → 模块 3
```

| 通信路径 | 方式 | 同步/异步 | 说明 |
|------|------|:--:|------|
| 模块 1 → 模块 2 | REST | 同步 | <!-- 说明 --> |
| 模块 1 → 模块 3 | 事件 | 异步 | <!-- 说明 --> |

---

## 4. 数据架构

### 数据库分布

| 模块 | 数据库 | 核心表 |
|------|------|------|
| <!-- 模块 1 --> | <!-- 同一 DB / 独立 DB --> | <!-- table_1, table_2 --> |

### 跨模块数据访问规则

- <!-- 例：模块 2 不直接读模块 1 的表，通过 API 获取 -->
- <!-- 例：模块 1 和模块 2 共享同一 DB，但不跨模块 join -->

---

## 5. 部署架构

```
<!-- 部署拓扑图（ASCII） -->

[LB] → [Service 1] × 2
     → [Service 2] × 1
            ↓
        [MySQL] [Redis]
```

---

## 6. 架构决策记录（ADR）

### ADR-1: <!-- 决策标题 -->

| 维度 | 说明 |
|------|------|
| **状态** | <!-- 提议 / 已确认 --> |
| **背景** | <!-- 为什么需要做这个决策 --> |
| **决策** | <!-- 选择了什么 --> |
| **理由** | <!-- 为什么选这个方案 --> |
| **替代方案** | <!-- 考虑过但没选的方案 + 不选的原因 --> |
| **影响** | <!-- 这个决策影响了哪些模块/任务 --> |

### ADR-2: <!-- 决策标题 -->

<!-- 同上 -->

---

## 7. 风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|:--:|:--:|------|
| <!-- 如：数据库性能瓶颈 --> | 高 | 中 | <!-- 提前做索引优化 + 读写分离预留 --> |
| <!-- 如：第三方 API 不稳定 --> | 中 | 高 | <!-- 加超时重试 + 降级策略 --> |
